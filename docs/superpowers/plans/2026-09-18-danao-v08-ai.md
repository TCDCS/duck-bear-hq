# Danao v0.8 AI and Content Depth Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Ship Danao v0.8 with safer, more varied bots that deliberately use props and the arena rather than only walking at the nearest opponent.

**Architecture:** Keep bot decisions in `bot.js` as pure deterministic math. Patch the packaged runtime at assembly time to provide live arena, hazard, prop and fighter state. Keep all bot actions routed through the same movement/attack/grab/dodge inputs used by players.

**Tech Stack:** JavaScript ES modules, Babylon.js, Rapier, Node test runner, Cloudflare Worker deployment.

**Spec:** `docs/superpowers/specs/2026-09-18-danao-v08-ai-design.md`

## Global Constraints

- No new modes, menus, accounts or multiplayer systems.
- No stat bonuses or hidden physics advantages for bots.
- Player controls remain unchanged.
- Bots must use existing attack/grab/dodge inputs only.
- Edge/hazard avoidance has higher priority than attacking.
- Runtime release packaging remains reproducible through `scripts/assemble-danao-v05.mjs`.
- Existing v0.7 visuals and Mango Mayhem cast identities must remain unchanged.

---

### Task 1: Pure bot decision model

**Files:** Modify `public/games/danao/src/game/bot.js`; create `tests/danao-v08.test.mjs`.

- [ ] Write failing tests for personality assignment, edge retreat, hazard avoidance, prop seeking, held-item throw intent and close-range pressure.
- [ ] Run the v0.8 tests and confirm RED.
- [ ] Implement `botProfileForSlot(slot)` and extend `botIntent(self, target, context, random)`.
- [ ] Preserve backward compatibility with the old `botIntent(self, target, random)` call shape.
- [ ] Run tests and confirm GREEN.

### Task 2: Packaged runtime integration

**Files:** Create `scripts/danao-v08-runtime-patch.mjs`; modify `scripts/assemble-danao-v05.mjs`, exact release hash assertions and `tests/danao-v08.test.mjs`.

- [ ] Add failing source-contract assertions for profile creation and live arena/hazard/prop context.
- [ ] Confirm RED after assembly.
- [ ] Patch the authoritative packaged runtime during assembly.
- [ ] Remove the legacy random bot-grab shortcut so grab/throw decisions come from `botIntent`.
- [ ] Update exact runtime SHA-256 and verify assembly/syntax/tests.

### Task 3: Wrestling Hall item depth

**Files:** Modify `public/games/danao/src/game/arena.js` and `tests/danao-v08.test.mjs`.

- [ ] Add a failing assertion requiring at least 24 Wrestling Hall props and a broader mix of chairs, cones, baguettes, mallets and crates.
- [ ] Add only existing balanced item definitions; do not change arena bounds or spawn points.
- [ ] Verify tests.

### Task 4: Release and live verification

**Files:** Modify `public/games/danao/release.json` and release assertions.

- [ ] Bump Danao to `0.8.0`.
- [ ] Run exact assembly, Danao syntax checks, full repository tests and repository checks.
- [ ] Review diff for unrelated changes.
- [ ] Merge only after green validation.
- [ ] Verify live `release.json` and start a bot-filled Wrestling Hall match in the browser.
