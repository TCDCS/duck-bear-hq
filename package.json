{
  "name": "duck-bear-hq",
  "version": "6.0.0",
  "private": true,
  "type": "module",
  "description": "Duck & Bear public website, private account and Wacky Races online rooms",
  "scripts": {
    "assemble:danao": "node scripts/assemble-danao-v05.mjs",
    "deploy": "npm run assemble:danao && npm test && npm run check && wrangler d1 migrations apply DB --remote && wrangler deploy",
    "check": "node scripts/check.mjs && node scripts/mango-check.mjs",
    "pretest": "npm run assemble:danao",
    "test": "node --test tests/*.cjs tests/*.mjs",
    "smoke": "node scripts/smoke.mjs",
    "dev": "wrangler dev",
    "migrate:local": "wrangler d1 migrations apply DB --local"
  },
  "devDependencies": {
    "wrangler": "^4.45.0"
  }
}
