/** Original synthesized soundtrack/effects; no copied transport recordings. */
export class AudioBus{
 constructor(settings){this.settings=settings;this.ctx=null;this.lastBeat=-1;this.lastFoot=-1;}
 unlock(){try{if(!this.ctx){const AC=window.AudioContext||window.webkitAudioContext;if(!AC)return;this.ctx=new AC();}if(this.ctx.state==='suspended')this.ctx.resume().catch(()=>{});}catch{this.ctx=null;}}
 tone(freq,duration=.15,level=.15,type='sine',music=false,delay=0){try{if(!this.ctx||this.ctx.state!=='running')return;const volume=music?this.settings.music:this.settings.effects;if(!volume)return;const o=this.ctx.createOscillator(),g=this.ctx.createGain(),t=this.ctx.currentTime+delay;o.type=type;o.frequency.setValueAtTime(freq,t);o.frequency.exponentialRampToValueAtTime(Math.max(35,freq*.7),t+duration);g.gain.setValueAtTime(.0001,t);g.gain.exponentialRampToValueAtTime(Math.max(.0001,level*volume),t+.012);g.gain.exponentialRampToValueAtTime(.0001,t+duration);o.connect(g);g.connect(this.ctx.destination);o.start(t);o.stop(t+duration+.02);o.onended=()=>{o.disconnect();g.disconnect();};}catch{}}
 receive(e){if(e.type==='jump')this.tone(410,.15,.10,'triangle');if(e.type==='hit')this.tone(140,.23,.12,'triangle');if(e.type==='doors')for(let i=0;i<3;i++)this.tone(660,.1,.11,'sine',false,i*.19);if(e.type==='board'||e.type==='win'){[523,659,784].forEach((f,i)=>this.tone(f,.3,.10,'sine',false,i*.13));}if(e.type==='miss'){[392,330,262].forEach((f,i)=>this.tone(f,.3,.09,'triangle',false,i*.17));}}
 update(run){if(run.phase!=='running')return;const beat=Math.floor(run.elapsed/1.3);if(beat!==this.lastBeat){this.lastBeat=beat;const notes=[196,246.94,293.66,246.94,174.61,220,261.63,220];this.tone(notes[beat%8],.7,.04,'triangle',true);this.tone(notes[beat%8]*2,.3,.018,'sine',true,.32);}const step=Math.floor(run.player.s/1.05);if(step!==this.lastFoot&&run.player.y===0){this.lastFoot=step;this.tone(80+step%2*20,.05,.035,'triangle');}}
 pause(){try{this.ctx?.suspend().catch(()=>{});}catch{}}
}
